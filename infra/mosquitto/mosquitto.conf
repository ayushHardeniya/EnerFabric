# Minimal local development broker configuration for EnerFabric.
# Anonymous access is fine for a hackathon MVP running on an isolated
# Docker network; do not reuse this configuration as-is in production.

listener 1883
allow_anonymous true

persistence false
