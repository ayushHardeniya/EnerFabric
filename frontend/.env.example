# Copy to .env.local for local development.

# Base URL of the EnerFabric backend (FastAPI). The WebSocket URL
# (ws://.../api/v1/ws) is derived from this at runtime — see src/lib/config.ts.
NEXT_PUBLIC_API_URL=http://localhost:8000
